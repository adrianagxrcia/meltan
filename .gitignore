# Artefactos de compilacion (se regeneran con idf.py build)
build/
*.o
*.a
*.bin
*.elf
*.map

# Configuracion generada (se reconstruye desde sdkconfig.defaults)
sdkconfig
sdkconfig.old

# Gestor de componentes de ESP-IDF
managed_components/
dependencies.lock

# Servidor de lenguaje / herramientas
.cache/
compile_commands.json

# Sistema operativo
.DS_Store
Thumbs.db
